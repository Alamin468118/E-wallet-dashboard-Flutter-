import 'package:flutter/material.dart';

// in this dart file, there will be has the color constant thathad been use in build this project

const kPrimaryColor =
    Color.fromARGB(255, 1, 21, 37); //primary color for this app
const kaTextColor = Colors.white30; // text color white
const kbTextColor = Colors.blueAccent; // text color blue
const kBackgroundColor = Colors.white; //background color
const kShadow = Colors.grey;

const double kDefaultPadding = 20.0; // padding
